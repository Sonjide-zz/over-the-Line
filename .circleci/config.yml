version: 2.1

jobs:
  build:
    working_directory: ~/backend
    docker:
      - image: circleci/node@3.0.0
    steps:
      - checkout
  build2:
    working_directory: ~/frontend
    docker:
      - image: circleci/node@3.0.0
    steps:
      - checkout

workflows:
  version: 2.1

  my_workflow:
    jobs:
      - build
      - build2:
          requires:
            - build
